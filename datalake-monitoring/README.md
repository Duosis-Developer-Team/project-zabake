# Datalake Monitoring

This module hosts reconciliation and consistency checks between Datalake and inventory sources.

Current scope:
- virtualization inventory reconciliation (VMware VM, Nutanix VM, IBM LPAR)
- AWX-compatible Ansible playbooks
- JSON reporting, optional email, optional DB upsert
